# v0.1.9 Release Report

Date: 2026-06-19
Release checkout: `/home/zzchen/Agent_virtuoso/EDA_AI_AGENT/ic-auto-opt-workflow-v0.1`

## Scope

v0.1.9 publishes the verified final release package that was prepared after the
v0.1.8 release line had already been tagged. The existing `v0.1.8` tag is not
moved. This release includes the final fix-run workflow sync, package-template
sync, documentation sync, and release hygiene checks.

Internal `docs/superpowers/` artifacts, `graphify-out/`, and local optimizer
research notes are excluded from the release package. `vendor/open-box/` is not
modified.

## Drift checks

- legacy doctor report path: no user-facing matches after release doc/skill
  update to `reports/ic_opt_doctor_report.json`.
- removed execution-agent CLI flag in non-test release docs: no user-facing
  matches; remaining test references are negative compatibility assertions.
- example/template mirrors:
  - `OPT_REQUIREMENT_README.md`: same
  - `opt_requirement.md`: same
  - `opt_requirement.multi_corner.md`: same
  - `opt_requirement.multi_testbench.md`: same
  - `opt_requirement.multi_tb_corner.md`: same
  - `opt_requirement.fix_run.md`: same
- packaged template config resources tracked:
  - `src/hermes_workflow/templates/spectre_maestro_project/config/project_config.yaml`
  - `src/hermes_workflow/templates/spectre_maestro_project/config/variables.yaml`
  - `src/hermes_workflow/templates/spectre_maestro_project/config/metrics.yaml`
  - `src/hermes_workflow/templates/spectre_maestro_project/config/spectre.yaml`
  - `src/hermes_workflow/templates/spectre_maestro_project/config/optimizer.yaml`
- release cache pollution: cleared; no `__pycache__`, `.pytest_cache`, or
  `.ruff_cache` entries remain in `git status --ignored`.
- version metadata:
  - `pyproject.toml`: `0.1.9`
  - `src/hermes_workflow/__init__.py`: `0.1.9`

## Verification

Because the release checkout has no local `.venv`, tests are run from the
release checkout with `PYTHONPATH=src` and the verified dev interpreter:
`/home/zzchen/Agent_virtuoso/EDA_AI_AGENT/ic-auto-opt-workflow/.venv/bin/python`.

- `PYTHONPATH=src <dev-venv-python> -m pytest tests/test_template_coupling_guard.py -q`
  -> `1 passed`
- `PYTHONPATH=src <dev-venv-python> -m pytest -q`
  -> `1194 passed, 13 warnings`
- `PYTHONPATH=src <dev-venv-python> -m ruff check src tests`
  -> `All checks passed!`
- `git diff --check`
  -> clean

## Boundary checks

- Existing `v0.1.8` tag: not moved.
- `docs/superpowers/`: excluded from release.
- `graphify-out/`: not copied, not staged, not present in release checkout.
- `vendor/open-box/`: unchanged.

## Status

Release v0.1.9 is verified and ready for tag/publication.
