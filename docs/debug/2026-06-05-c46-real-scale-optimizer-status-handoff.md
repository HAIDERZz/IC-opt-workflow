# C-46 Real-Scale Optimizer Status Handoff

Date: 2026-06-05

## Purpose

Run the current production OpenBox handoff at a realistic 100-evaluation scale
from a fresh workspace and close it out with the C-44 `optimizer-status`
handoff flow.

## Workspace

```text
/tmp/ic_auto_opt_c46_real_scale_status_handoff_001/bridge_test_inv
```

Source for clean project inputs:

```text
/tmp/ic_auto_opt_c34_clean2/bridge_test_inv
```

Only `config/`, `netlists/`, and matching `supervisor_instruction.json` were
copied from the source. Old `ledger/`, `state/`, `reports/`, and `runs/` were
not copied.

## Packet Evidence

Generated by:

```bash
.venv/bin/hermes-workflow package-optimizer-task /tmp/ic_auto_opt_c46_real_scale_status_handoff_001/bridge_test_inv --backend openbox --max-evals 100 --cadence-cshrc /home/zzchen/cadence_ic231_env.csh --parallel
```

The generated packet contained:

- backend `openbox`
- `run-openbox-real`
- `max_evals=100`
- `batch_size=10`
- `parallel_jobs=10`
- `threads_per_run=10`
- `check-toolchain-env`
- `optimizer-status` in task text and manifest `audit_commands`

## Real Execution Evidence

Toolchain gate:

```text
toolchain environment check passed
```

Real command shape:

```bash
csh -fc 'source /home/zzchen/cadence_ic231_env.csh; setenv PATH /tmp/ic_auto_opt_openbox_spike/.venv/bin:$PATH; setenv MPLCONFIGDIR /tmp/ic_auto_opt_c46_real_scale_status_handoff_001/mpl_cache; cd /home/zzchen/Agent_virtuoso/EDA_AI_AGENT/ic-auto-opt-workflow; hermes-workflow run-openbox-real /tmp/ic_auto_opt_c46_real_scale_status_handoff_001/bridge_test_inv --max-evals 100 --batch-size 10 --parallel-jobs 10 --cadence-cshrc /home/zzchen/cadence_ic231_env.csh'
```

Result:

```text
openbox real optimization completed: 100 evaluations
```

## Closeout Summary

Audit commands:

```bash
.venv/bin/hermes-workflow check-optimizer-run /tmp/ic_auto_opt_c46_real_scale_status_handoff_001/bridge_test_inv
.venv/bin/hermes-workflow summarize-optimizer-run /tmp/ic_auto_opt_c46_real_scale_status_handoff_001/bridge_test_inv
.venv/bin/hermes-workflow finalize-optimizer-run /tmp/ic_auto_opt_c46_real_scale_status_handoff_001/bridge_test_inv
.venv/bin/hermes-workflow optimizer-status /tmp/ic_auto_opt_c46_real_scale_status_handoff_001/bridge_test_inv
```

Result summary:

```text
evaluation_count: 100
batch_count: 10
max_batch_worker_count: 10
status counts: feasible=43, constraint_failed=51, metric_check_failed=6
best observed: real_071
best parameters: FN=12, WN=2.7u, FP=7, WP=0.7u
best metrics: rise=69.604322ps, fall=56.781892ps, DC=326.978183uW
best objective: 4.1325534822170306e-14
decision: accept_best_observed
confidence: medium
global_optimum_claim: false
continuation recommended: false
plateau_detected: true
parallel_jobs: 10
threads_per_run: 10
```

## Notes

- This C-46 real-scale run reproduces the known deterministic C34 100-eval
  OpenBox result on a fresh workspace with the updated packet/status closeout.
- No unexpected package, tool, license, Spectre, OCEAN, or metric extraction
  infrastructure failure appeared.
- Candidate `constraint_failed` and `metric_check_failed` statuses were treated
  as optimizer outcomes, not toolchain failures.
- Raw Cadence outputs, PSF/raw data, protected sidecars, and full logs remain in
  `/tmp` only.
