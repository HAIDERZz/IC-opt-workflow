# Optional OpenBox advanced visualization dependencies.
# Base optimization does not require these packages.
#
# Install only if you need OpenBox advanced surrogate verification or
# hyperparameter-importance views.
#
# If pyrfr fails with "fatal error: Python.h: No such file or directory",
# Python development headers are missing. Ask an administrator for
# python3.11-devel on RHEL-like systems or python3.11-dev / python3-dev on
# Debian-like systems.
#
# swig must already be on PATH before the second line below runs: pyrfr
# builds from source and needs swig at build time, and pip does not
# guarantee installing this file's entries in order. The pyrfr/shap/
# lightgbm version pins live in pyproject.toml's `[project.optional-
# dependencies].advanced` extra, which is the sole authority for those
# pins; swig stays pinned here (not in extras) only because of this
# must-precede-pyrfr build-order constraint. `-e .[advanced]` (not
# `.[advanced]`) keeps the package installed editable, matching the `-e .`
# install from requirements-product.txt instead of overwriting it.

swig==4.4.1
-e .[advanced]
