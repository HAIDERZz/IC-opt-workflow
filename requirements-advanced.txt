# Optional OpenBox advanced visualization dependencies.
#
# Base optimization does not require this file. Install it only if you want
# OpenBox's advanced surrogate verification / hyperparameter importance views.
#
# If pyrfr fails with "fatal error: Python.h: No such file or directory", your
# Python development headers are missing. Ask your administrator for the package
# matching the Python used by your .venv, for example python3.11-devel on
# RHEL-like systems or python3.11-dev / python3-dev on Debian-like systems.

swig==4.4.1
pyrfr==0.9.0
shap==0.49.1
lightgbm==4.6.0
