# Product-level optimizer environment.
#
# Install from the release root:
#   python3 -m venv .venv
#   ./.venv/bin/python -m pip install --upgrade pip setuptools wheel
#   ./.venv/bin/python -m pip install -r requirements-product.txt
#
# User Spectre optimization project directories stay data-only. Do not create a
# Python virtualenv inside a user optimization project.

-e .
-e vendor/open-box
# TuRBO is not pip-installed here: hermes_workflow resolves it at runtime
# from vendor/TuRBO (or $TURBO_HOME) via DEFAULT_TURBO_PATH, which every
# native TuRBO entry point inserts into sys.path before importing turbo
# (see src/hermes_workflow/native_turbo.py). No editable install needed.
-r vendor/open-box/requirements/main.txt

# Library version pins (matplotlib, scipy, threadpoolctl, torch, gpytorch)
# are not repeated here. `-e .` installs them from pyproject.toml's
# `dependencies` array, which is the sole authority for those pins.
