# Product-level optimizer environment.
#
# Usage from the ic-auto-opt-workflow repository root:
#   python3 -m venv .venv
#   ./.venv/bin/python -m pip install --upgrade pip setuptools wheel
#   ./.venv/bin/python -m pip install -r requirements-product.txt
#
# User Spectre optimization projects stay data-only. Do not create a Python venv
# inside each user project directory.

-e .
-e vendor/open-box
-r vendor/open-box/requirements/main.txt

swig==4.4.1
pyrfr==0.9.0
shap==0.49.1
lightgbm==4.6.0
