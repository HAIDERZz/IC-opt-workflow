# Product-level optimizer environment.
#
# Install from the release root:
#   python3 -m venv .venv
#   ./.venv/bin/python -m pip install --upgrade pip setuptools wheel
#   ./.venv/bin/python -m pip install -r requirements-product.txt
#
# User Spectre optimization project directories stay data-only. Do not create a
# Python virtualenv inside a user optimization project.

-e .
-e vendor/open-box
-e vendor/TuRBO
-r vendor/open-box/requirements/main.txt

matplotlib>=3.7
scipy>=1.10
threadpoolctl>=3.1
torch>=2.0
gpytorch>=1.11
